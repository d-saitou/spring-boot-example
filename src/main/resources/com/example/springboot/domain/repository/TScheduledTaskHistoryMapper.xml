<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.springboot.domain.repository.TScheduledTaskHistoryMapper">
  <insert id="create" parameterType="com.example.springboot.domain.entity.TScheduledTaskHistory">
    INSERT INTO t_scheduledtask_history (method, message, update_date) VALUES (#{method}, #{message}, #{updateDate})
  </insert>

  <select id="findAllOrderById" resultType="com.example.springboot.domain.entity.TScheduledTaskHistory">
    SELECT
      id, method, message, update_date
    FROM
      t_scheduledtask_history
    ORDER BY
      id
  </select>
</mapper>
